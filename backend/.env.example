# Server
PORT=5000
NODE_ENV=development

# Database - configure these when we add MySQL in the next step.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=authentiscan_db
DB_USER=root
DB_PASSWORD=

# Future service settings - do not put real secrets in Git.
JWT_SECRET=replace-with-a-long-random-secret
AI_SERVICE_URL=http://127.0.0.1:5001
